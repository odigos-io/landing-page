export const metadata = {
  title: 'Odigos CMS Studio',
  description: 'Content management for the Odigos landing page',
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
